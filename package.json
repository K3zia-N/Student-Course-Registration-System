{
  "name": "student-course-registration",
  "version": "1.0.0",
  "description": "Student Course Registration System",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/main/resources/static/* dist/ && cp -r src/main/resources/templates/* dist/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "24.x"
  }
}
