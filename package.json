{
  "name": "student-course-registration",
  "version": "1.0.0",
  "description": "Student Course Registration System",
  "scripts": {
    "build": "echo 'Building Spring Boot application...' && mvn clean package -DskipTests",
    "start": "java -jar target/studentapp-0.0.1-SNAPSHOT.jar"
  },
  "engines": {
    "node": "18.x"
  }
}
